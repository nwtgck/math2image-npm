# (from: https://github.com/codecov/example-node/blob/master/.travis.yml)
language: node_js
sudo: false
node_js:
  - "6"
install:
  - npm install
script:
  - npm run build
  - npm link

  # ==== Just working-test ====

  # Output to stdout
  - echo "e^{i \pi} + 1 = 0" | math2image